<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css">
    <title>Document</title>
</head>
<body>
    <div class="container pt-3 mx-3 d-flex align-items-center justify-content-start">
        <div class="card">
            <img style="height: 300px; width: fit-content;" src="<%=foundItem.itemImg%>" class="card-img-top" alt="...">
                <div class="card-body">
                  <h5 class="card-title"><%=foundItem.itemName%></h5>
                  <p class="card-text"><%=foundItem.itemDes%></p>
                  <h4>₹ <%=foundItem.itemPrice%>.00/-</h4>
                  <% if(LoggedInUser.cart.indexOf(foundItem._id) === -1){ %>
                    <a href="/add/<%=foundItem._id%>" class="btn btn-primary">Add to Cart</a>
                  <% } else{ %>
                    <div>
                        <a href="/add/<%=foundItem._id%>" class="incart btn btn-warning">Add to Cart</a>
                        <a href="/cart" class="btn btn-primary">Go to Cart</a>
                    </div>
                    <div class="d-flex justify-content-between">
                        <pre class="mt-3">Already in cart</pre>
                        <pre class="mt-3">QTY: <%=foundItem.qty%></pre>
                    </div>
                  <% } %>
            </div>
        </div>
    </div>
</body>
</html>